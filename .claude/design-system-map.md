# Design System Map

Inventory of every UI component in use across the codebase, grouped by source
library, with file path and usage count. Generated by searching all imports
under `components/`, `app/`, `lib/`, and `types/` (excluding `node_modules`).

## Naming note: "Radian"

"Radian" is the name of the Figma design-system library this codebase
implements (`.cursor/rules/providhy-radian-design-system.mdc`). In code,
Radian = `components/ui/**`. It is built primarily on **Base UI**
(`@base-ui/react`), with a handful of gaps filled by **Radix UI** primitives.

---

## 1. Radian design system (`components/ui/**`) — the shared library

Source of truth per `CLAUDE.md`. All feature/page code should import these
rather than hand-rolling equivalents.

| Component | Path | Used in (files) |
|---|---|---|
| Button | `components/ui/button.tsx` | 194 |
| Dropdown Menu | `components/ui/dropdown-menu.tsx` | 81 |
| Tooltip | `components/ui/tooltip.tsx` | 54 |
| Button Group | `components/ui/button-group.tsx` | 46 |
| Input | `components/ui/input.tsx` | 44 |
| Badge | `components/ui/badge.tsx` | 32 |
| Form | `components/ui/form.tsx` | 29 |
| Card | `components/ui/card.tsx` | 27 |
| Dialog | `components/ui/dialog.tsx` | 27 |
| Checkbox | `components/ui/checkbox.tsx` | 25 |
| Tabs | `components/ui/tabs.tsx` | 25 |
| Spinner | `components/ui/spinner.tsx` | 18 |
| Textarea | `components/ui/textarea.tsx` | 18 |
| Form Dialog | `components/ui/form-dialog.tsx` | 15 |
| Native Select | `components/ui/native-select.tsx` | 15 |
| Popover | `components/ui/popover.tsx` | 13 |
| Command | `components/ui/command.tsx` | 12 |
| Avatar | `components/ui/avatar.tsx` | 6 |
| Separator | `components/ui/separator.tsx` | 5 |
| Label | `components/ui/label.tsx` | 4 |
| Sidebar | `components/ui/sidebar.tsx` | 4 |
| Switch | `components/ui/switch.tsx` | 4 |
| Chart | `components/ui/chart.tsx` | 4 |
| Sheet | `components/ui/sheet.tsx` | 3 |
| Collapsible | `components/ui/collapsible.tsx` | 2 |
| Breadcrumb | `components/ui/breadcrumb.tsx` | 1 |
| Carousel | `components/ui/carousel.tsx` | 1 |
| Drawer | `components/ui/drawer.tsx` | 1 |
| Input Group | `components/ui/input-group.tsx` | 1 |
| OTP Field | `components/ui/otp-field.tsx` | 1 |
| Skeleton | `components/ui/skeleton.tsx` | 1 |
| Sonner (`Toaster` + `toast()`) | `components/ui/sonner.tsx` | 95 (1 `Toaster` mount + 94 `toast()` call sites) |
| Divider | `components/ui/divider.tsx` | 0 — unused, dead file |

### Underlying primitives (wrapped by the above; never imported directly by feature code)

Confirmed via repo-wide search: zero direct imports of `@base-ui/react/*` or
`@radix-ui/*` outside `components/ui/`. Rule #1 in `CLAUDE.md` ("search
`components/ui/` first") is being followed in practice. This layer is
invisible to Figma parity — Radian's Figma fidelity is governed by the
`components/ui/*` component API, not by which headless engine sits under it.

- **Base UI** (`@base-ui/react/*`) — used in 19 wrapper files: `tabs`,
  `switch`, `tooltip`, `sidebar`, `popover`, `separator`, `sheet`, `input`,
  `drawer`, `dropdown-menu`, `dialog`, `collapsible`, `button`, `avatar`,
  `badge`, `breadcrumb`, `button-group`, `divider`, `otp-field`.
- **Radix UI** — 1 wrapper file remains, kept deliberately: `form.tsx`
  (`react-slot`, via `FormControl`).
- **cmdk** — `components/ui/command.tsx` only.

**Consolidation status (updated 2026-09-18):**

- **Done** — `divider.tsx` migrated to `@base-ui/react/separator` (was
  0-usage/dead, zero risk). `otp-field.tsx` migrated to
  `@base-ui/react/otp-field`; its one call site
  (`components/verification-form.tsx`, the email-verification/auth flow)
  was updated to match Base UI's API differences: `index` is now inferred
  automatically from render order (no longer passed as a prop), an explicit
  `length={6}` is now required on `<OTPField>` (Base UI doesn't infer it
  from children count the way Radix did), and `OTPHiddenInput` was removed
  since Base UI's `OTPFieldRoot` has no separate hidden-input part — this
  form is fully react-hook-form-controlled via `value`/`onValueChange`, so
  no native-form submission behavior was lost. Verified via `tsc --noEmit`,
  `eslint`, and confirming SSR output renders all 6 slots with no error
  overlay (an already-running dev server on the machine made a full
  interactive browser click-through impractical to script without
  disrupting that session — worth a manual click-through of `/verification`
  before shipping).
- **Also done (2026-09-18)** — centralized `toast()` under Radian.
  `components/ui/sonner.tsx` previously only wrapped the `<Toaster>`
  container; the `toast()` function itself was imported straight from
  `"sonner"` in 94 feature files, bypassing the design-system layer
  entirely. Added `export { toast } from "sonner"` to
  `components/ui/sonner.tsx` and repointed all 94 call sites to
  `import { toast } from "@/components/ui/sonner"`. Purely a re-export +
  import-path change — no behavior, styling, or call-signature changes —
  verified via `tsc --noEmit`, `eslint` across all 94 files, and a full
  `npm run build`, all clean. This closes the gap: if Radian's toast
  variant/icon/duration conventions ever change, there is now one file to
  edit instead of 94.
- **Also done (2026-09-18)** — `label.tsx` dropped `@radix-ui/react-label`
  entirely rather than migrating to Base UI's `Field.Label`. `Field.Label`
  requires a `Field.Root`/`Labelable` context ancestor (it throws without
  one — confirmed by reading `FieldRootContext.js`'s `useFieldRootContext`),
  and none of this app's 3 real call sites (`stock-transfer-2`,
  `stock-transfer`, `permission-management-page`) use `Field.Root` — each
  just pairs a plain `<Label htmlFor="...">` with a matching input id.
  Reading Radix's own source showed `Label.Root` is just a native `<label>`
  plus an `onMouseDown` handler that blocks text-selection on rapid clicks
  (skipped when the click lands on an interactive child), so that behavior
  was hand-rolled directly in `label.tsx` — same precedent as
  `checkbox.tsx` being hand-built rather than wrapping a headless
  primitive. `@radix-ui/react-label` was removed from `package.json`.
  Verified via `tsc --noEmit`, `eslint`, and `npm run build`, all clean.
- **Deliberately not done** — `form.tsx`'s `FormControl`. It relies on
  Radix's `Slot` implicit-`asChild` behavior
  (`<FormControl><Input/></FormControl>` merges props onto the child
  automatically); Base UI's `useRender` equivalent needs an explicit
  `render` prop instead, which would require editing call sites in all
  ~30 forms that use `FormControl`, not just the primitive underneath. A
  real, larger project — left for a dedicated branch with per-form
  testing rather than folded into this pass.

---

## 2. Other libraries (imported directly by app/feature code)

| Library | Purpose | Usage |
|---|---|---|
| `lucide-react` | icon set | 187 files |
| `@tanstack/react-table` | table engine | 35 files — centered in `components/data-table/*` and `*-columns.tsx` files across every module |
| `react-hook-form` | form state | 30 files |
| `@hugeicons/react` | icon set (secondary) | 15 files |
| `recharts` | charts | 5 files — 4 direct in dashboard widgets (`cash-in-out.tsx`, `financial-insight.tsx`, `receivable-ageing.tsx`, `receivables-payables.tsx`) + `components/ui/chart.tsx` wrapper |
| `next-themes` | theme provider | 5 files (`theme-provider.tsx`, `mode-toggle.tsx`, `app-navbar.tsx`, `appearance-settings-panel.tsx`, `components/ui/sonner.tsx`) |
| `embla-carousel*` | carousel engine | 2 files (`components/ui/carousel.tsx`, `components/testimonial-carousel.tsx`) |

### Removed (were dead weight — zero imports anywhere in the codebase)

**Status: done, 2026-09-18.** Uninstalled from `package.json` and
`node_modules` (269 packages dropped from the tree). `npm run build` and
`npm run lint` were re-run after removal — build is clean; lint shows only
pre-existing, unrelated issues (`hooks/use-mobile.ts`,
`lib/companies/options.ts`) untouched by this change.

- `@carbon/react`
- `@chakra-ui/react`
- `@saas-ui/react`
- `@emotion/react` (was only Chakra's peer dependency)
- `@radix-ui/react-checkbox` (note: `components/ui/checkbox.tsx` is actually
  hand-built from scratch, not built on this package, so nothing had to
  change in the component itself)

---

## 3. Custom-built (bespoke, app-specific — not from any UI library)

- **`components/data-table/**`** — full custom data-table system
  (`data-table.tsx`, `data-table-toolbar.tsx`, `data-table-pagination.tsx`,
  `data-table-column-header.tsx`, `data-table-view.tsx`,
  `data-table-card.tsx`, `use-data-table.ts`) wrapping
  `@tanstack/react-table`. Used by 145 files.
- **`components/reports/*-table.tsx`** — 48 report-specific table
  components (trial balance, ledgers, ageing reports, etc.), each built on
  top of the shared data-table primitives.
- **`components/reui/icon-stack.tsx`** — 1 bespoke icon component, used in
  1 place.

### Known design-system violations (still unresolved)

Per `CLAUDE.md`'s "Known past violations" list — both files are still
present in the repo and still do not import `components/ui/tabs`, in
violation of `.cursor/rules/ui-tabs.mdc`:

- `components/dashboard/home/dashboard-widget-tabs.tsx` — hand-rolled tab
  bar.
- `components/settings/appearance-settings-panel.tsx` — hand-rolled
  segmented control.

---

## Summary

- The Radian design system (`components/ui/**`) is the dominant, correctly
  centralized source of shared UI, and its Base UI/Radix primitives are
  never leaked into feature code — the "search `components/ui/` first" rule
  is holding.
- Feature code reaches directly for a small, consistent set of
  non-visual/utility libraries (icons, forms, tables, toasts, charts,
  theming) rather than competing component kits.
- Three dead component libraries (`@carbon/react`, `@chakra-ui/react`,
  `@saas-ui/react`, plus `@emotion/react` and the unused
  `@radix-ui/react-checkbox`) have been removed from `package.json` —
  they were never imported and added no real risk to Figma parity, just
  install weight.
- Two known violations (hand-rolled tabs) remain unfixed and should migrate
  to `components/ui/tabs` per `.cursor/rules/ui-tabs.mdc`.
- `divider.tsx` and `otp-field.tsx` are now on Base UI, `toast()` is now
  centralized through `components/ui/sonner.tsx` instead of being imported
  from `sonner` directly in 94 places, and `label.tsx` has dropped
  `@radix-ui/react-label` in favor of a hand-rolled native `<label>` — see
  the "Consolidation status" notes above. `form.tsx`'s `FormControl` is the
  last Radix-only wrapper, remaining deliberately, pending a dedicated
  branch (see "Deliberately not done" above).
